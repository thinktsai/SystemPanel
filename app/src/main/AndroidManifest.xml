<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ctos.systempanel" >

    <uses-permission android:name="android.permission.SET_TIME_ZONE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/NormalActivity.Light" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.ctos.systempanel.AppsSelectionActivity"
            android:enabled="true"
            android:label="@string/app_name"
            android:theme="@style/NormalActivity.Light" />
        <activity
            android:name="com.ctos.systempanel.TimeZoneSelectionActivity"
            android:enabled="true"
            android:label="@string/app_name"
            android:theme="@style/NormalActivity.Light" />
        <activity
            android:name="com.ctos.systempanel.SignupActivity"
            android:enabled="true"
            android:label="@string/app_name"
            android:theme="@style/NormalActivity.Light" />


    </application>

</manifest>
